# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
remotego

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# remotego sqlite3 database
remotego.db
